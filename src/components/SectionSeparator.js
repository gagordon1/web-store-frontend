

import styled from 'styled-components';
import { colors } from '../style/Theme'

export const SectionSeparator = styled.div`
  position: relative;
  width: 100%;
  height: 0px;
  border: 0.1px solid ${colors.backgroundLineColor};
`
